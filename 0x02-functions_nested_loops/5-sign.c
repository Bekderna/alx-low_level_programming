#include <time.h>
#include <stdio.h>
/**
 * print_sign- prints alphabet
 *  @n: num
 *    * Return: int  (Success)
 */
int print_sign(int n)
{
	if (n >= 1)
	{
		putchar('+');
		return (1);
	}
	else if (c > 0)
	{
		putchar('-');
		return ("-1");
	}
	else
	{
		putchar('0');
		return (0);
	}
}
