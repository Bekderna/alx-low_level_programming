#include <stdio.h>
/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (success)
 */
int main(void)
{
	if (n > 0)
	{
			printf("is positive");
	}
		else if (n < 0)
		{
			printf("is negative");
		}
		else
		{
			printf("is zero");
		}
		return (0);
}
