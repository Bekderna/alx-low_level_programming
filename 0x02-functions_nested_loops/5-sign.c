#include <main.h>
#include <stdio.h>
/**
 * print_sign- prints alphabet
 *  @n: num
 *    * Return: int  (Success)
 */
int print_sign(int n)
{
	if (n >= 1)
	{
		_putchar('+');
		put;
	}
	else if (n < 0)
	{
		_putchar('-');
		return (-1);
	}
	else
	{
		_putchar('0');
		return (0);
	}
}
