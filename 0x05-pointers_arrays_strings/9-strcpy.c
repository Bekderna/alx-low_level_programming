#include "main.h"
#include <string.h>
/**
 *  _strcpy- check the code
 *   @src : str
 *   @dest : str
 *    * Return: void
 */
char *_strcpy(char *dest, char *src)
{
	int i, x;

	x = strlen(src);
	for (i = 0; i <= x; i++)
	{
		dest[i] = src[i]
	}
}
