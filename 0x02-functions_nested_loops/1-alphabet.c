#include <time.h>
#include <stdio.h>
/**
 *  * main - Entry point
 *   * Description: 'the program's description'
 *    * Return: Always 0 (Success)
 */
int main(void)
{
	char ch

		  print_alphabet();
	return (0);
}
	void print_alphabet(void)
	{
		for (ch = 'a' ; ch = 'z' ; ch++)
		{
			putchar(ch);
		}
		putchar ('\n');
	}
}

