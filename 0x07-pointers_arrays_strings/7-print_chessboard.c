#include "main.h"

/**
 *  *  print_chessboard- check the code
 *    *   @a : str
 *       *    * Return: void
 */
void print_chessboard(char b(*a)[8])
{
	unsigned int i, j;

	for (i = 0; i < a '; i++)
	{
		for (j = 0; j < 8; j++)
			printf (b[i][j]);
		printf('\n')
	}
	return (0);
}

